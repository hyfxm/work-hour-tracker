# 本地同步（推荐 · 无需服务器）

把工时数据在**不联网、不依赖任何云服务**的情况下，从一台设备传到另一台。原理：把本机数据编码成一串「导入码」，用二维码或文本在设备间传递，对方扫码/粘贴即写入。

## 为什么用这个而不是云同步

- **零服务器**：不需要注册 Supabase、不需要配置任何后端，纯本地。
- **零账号**：没有「同步码」「Anon Key」等概念，不用记任何东西。
- **隐私好**：数据不经过任何第三方，只在你两台设备之间。

> 如果你有多台设备**经常自动实时互相同步**的需求，才需要看 `CLOUD.md`（云端同步）。大多数个人用户用本地同步就够了。

## 怎么用

### A 设备（导出）

1. 打开工时助手 → 底部「规则」页 → **「数据同步」**。
2. 点 **「生成导出二维码」**，屏幕上出现一个二维码（已包含本机全部工时数据）。
3. 让 B 设备扫这个码即可；或点 **「复制导入码」** 把那串文本复制下来，用微信等发给对方。

### B 设备（导入）

1. 打开工时助手 → 底部「规则」页 → **「数据同步」**。
2. 选一种方式：
   - **相机扫码**：点「相机扫码」，把 A 设备的二维码对准摄像头（尽量占满屏幕），自动识别并写入。
   - **粘贴导入码**：把 A 设备「复制导入码」得到的文本粘贴进框，点「导入」。
3. 导入后与本机已有数据**自动合并**（同一天以本机为准），不会互相覆盖丢失。

## 注意事项

- **相机扫码需 HTTPS**：用 GitHub Pages 链接（`https://你的用户名.github.io/...`）打开时可用；本地 `file://` 打开或 HTTP 明文链接时浏览器会禁用摄像头，这种情况请用「复制导入码 + 粘贴」。
- **授权相机**：首次扫码浏览器会请求摄像头权限，需允许。
- **识别困难时**：若二维码太密（数据较多的月份）相机一时扫不出，直接用「复制导入码」文本粘贴，100% 可靠。
- **容量**：单张二维码可容纳约一个月的完整数据。若历史记录特别多，建议先清空很旧的月份再导出，或改用 `CLOUD.md` 云端同步。
- **安全**：导入码就是你的全部工时数据明文（仅时间，不含其他隐私），截屏或转发时注意别泄露给无关人员。
